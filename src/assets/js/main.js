// Import all of Bootstrap's JS
import * as bootstrap from 'bootstrap'

document.getElementsByName("logo").forEach((img) => {
    img.style.filter="invert(100%)";
});
// document.getElementsByTagName("body").innerHTML = `

// `;